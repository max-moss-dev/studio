{
  "id": "status-board",
  "title": "Status Board",
  "icon": "sparkles",
  "description": "A simple agent status overview with online/offline counts",
  "author": "studio",
  "version": "0.1.0",
  "code": "function View({ agents }) {\n  const online = agents.filter(a => a.status === 'online').length;\n  const total = agents.length;\n\n  return React.createElement('div', { style: { padding: 32, fontFamily: 'sans-serif' } },\n    React.createElement('h2', { style: { fontSize: 24, fontWeight: 600, marginBottom: 16 } }, 'Status Board'),\n    React.createElement('div', { style: { display: 'flex', gap: 16 } },\n      React.createElement('div', { style: { background: '#1a1a2e', borderRadius: 12, padding: 24, flex: 1 } },\n        React.createElement('div', { style: { fontSize: 36, fontWeight: 700, color: '#4ade80' } }, online),\n        React.createElement('div', { style: { fontSize: 14, color: '#888', marginTop: 4 } }, 'Online')\n      ),\n      React.createElement('div', { style: { background: '#1a1a2e', borderRadius: 12, padding: 24, flex: 1 } },\n        React.createElement('div', { style: { fontSize: 36, fontWeight: 700, color: '#f97316' } }, total - online),\n        React.createElement('div', { style: { fontSize: 14, color: '#888', marginTop: 4 } }, 'Offline')\n      ),\n      React.createElement('div', { style: { background: '#1a1a2e', borderRadius: 12, padding: 24, flex: 1 } },\n        React.createElement('div', { style: { fontSize: 36, fontWeight: 700, color: '#60a5fa' } }, total),\n        React.createElement('div', { style: { fontSize: 14, color: '#888', marginTop: 4 } }, 'Total Agents')\n      )\n    ),\n    React.createElement('div', { style: { marginTop: 24 } },\n      agents.map(function(agent) {\n        return React.createElement('div', { key: agent.id, style: { display: 'flex', alignItems: 'center', gap: 8, padding: '8px 0', borderBottom: '1px solid #222' } },\n          React.createElement('div', { style: { width: 8, height: 8, borderRadius: '50%', background: agent.status === 'online' ? '#4ade80' : '#666' } }),\n          React.createElement('span', { style: { fontSize: 14 } }, agent.name),\n          React.createElement('span', { style: { fontSize: 12, color: '#888', marginLeft: 'auto' } }, agent.status)\n        );\n      })\n    )\n  );\n}"
}
